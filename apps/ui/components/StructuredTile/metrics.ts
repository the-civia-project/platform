/** Hairline-bordered attachment tile (Poll, Petition, …). */
export const STRUCTURED_TILE_ATTACHMENT_BORDER_RADIUS_PX = 16;

/** Record / civic disclosure tile (Endorsement, Commitment, …). */
export const STRUCTURED_TILE_RECORD_BORDER_RADIUS_PX = 12;

/** Inner padding for every structured tile variant (logical px). */
export const STRUCTURED_TILE_PADDING_PX = 12;

/** Default vertical gap inside attachment tiles. */
export const STRUCTURED_TILE_ATTACHMENT_GAP_PX = 10;

/** Default vertical gap inside record / teaser tiles. */
export const STRUCTURED_TILE_RECORD_GAP_PX = 8;
