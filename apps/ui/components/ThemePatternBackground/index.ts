/**
 * Barrel for the shell backdrop: default {@link ./ThemePatternBackground} and
 * {@link ./hex-to-rgba}.
 * Import the default component from this folder in app shell code and UI Kit previews.
 */
export { hexToRgba } from "./hex-to-rgba";
export {
  default,
  type ThemePatternBackgroundProps,
  type PatternInk,
} from "./ThemePatternBackground";
