/**
 * Barrel for the kit's `Petition` family. Default-exported principal
 * (matches {@link "../Poll".default} / {@link "../Event".default}).
 */
export {
  default,
  Petition,
  type PostPetition,
  type PetitionProps,
} from "./Petition";
