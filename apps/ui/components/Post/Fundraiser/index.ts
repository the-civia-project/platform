/**
 * Barrel for the kit's `Fundraiser` family. Default-exported
 * principal (matches the other structured-tile families).
 */
export {
  default,
  Fundraiser,
  type PostFundraiser,
  type FundraiserProps,
} from "./Fundraiser";
