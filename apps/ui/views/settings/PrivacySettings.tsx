import { PlaceholderPage } from "../../core/placeholder/PlaceholderPage";

export default function PrivacySettings() {
  return (
    <PlaceholderPage
      lede="Blocking, muting, and data controls."
      sections={[
        {
          title: "Planned",
          body: "Blocked accounts, muted words, visibility defaults, and data export.",
        },
      ]}
    />
  );
}
