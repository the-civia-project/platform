import { PlaceholderPage } from "../core/placeholder/PlaceholderPage";

export default function Explore() {
  return (
    <PlaceholderPage
      lede="Search and discovery across the platform. Results and filters are not connected yet."
      sections={[
        {
          title: "Planned",
          body: "Query bar, results, and topic pages — wired from the home aside and footer search tab.",
        },
      ]}
    />
  );
}
