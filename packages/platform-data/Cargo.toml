[package]
name = "platform-data"
version = "0.0.1"
edition = "2024"

[dependencies]
rand = "0.10.0"

[build-dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
